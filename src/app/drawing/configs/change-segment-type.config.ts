import { SelectConfig } from "../components/segment-field/field-config.model";

const ChangeSegmentTypeConfig: SelectConfig = {
    data: [
        {
            display: "Arco",
            value: "Arc"
        },
        {
            display: "Linha",
            value: "Line"
        }
    ],
    type: "select"
}

export default ChangeSegmentTypeConfig